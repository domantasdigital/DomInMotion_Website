Portfolio preview images are generated from the original media with FFmpeg.
These small WebP files keep the grid from loading full videos or large PNGs.
Names include the original extension so 2.png and 2.mp4 stay separate.
You can override any preview using `thumbnail` in `lib/portfolio.js`.
